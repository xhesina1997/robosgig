[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm prisma generate && pnpm nx build api --prod"

[deploy]
startCommand = "node dist/apps/api/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
