[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm prisma generate && pnpm nx build api --prod && pnpm prisma migrate deploy"

[deploy]
startCommand = "node apps/api/dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
